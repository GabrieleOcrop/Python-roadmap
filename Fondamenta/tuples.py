#le tuple non possono essere modificate!

numbers = (1, 2, 3)

print(numbers[0])

#unpacking

coordinates = (12, 42, 1)

#shortcut per estrarre le variabili contenute in una tupla
x, y, z = coordinates

print(z)