prices = [10, 25, 4, 6.5,2 , 9.9, 14, 2]
for item, prices in enumerate(prices):
    if prices >= 5:
        print(f'The {item} item item that cost {prices} it too expensive')
    else:
        print(f'The {item} item that cost {prices} is cheaper')

#() calls a function
#[] join an element by an list, string or dictionary
#{} creates a set or a dictionary